machine:
  services:
    - docker

dependencies:
  cache_directories:
    - "vendor/docker"
  override:
    - ./script/bootstrap

test:
  override:
    - ./script/build
    - ./script/test

deployment:
  deploy:
    branch: master
    owner: bioboxes
    commands:
      - ./script/publish
      - ./plumbing/rebuild-website
